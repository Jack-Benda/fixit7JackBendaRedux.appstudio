let quoteString = "Failure is simply the opportunity to begin again this time more intelligently"
let upperCaseString = quoteString.toUpperCase()
alert("Upper case string is:" + quoteString)

let authorString = "-Henry Ford"
let completeString = quoteString.concat(authorString)

let secondQuote = "Talent is cheaper than table salt. What seperates the talented individual from the successful one is a lot fo hard work. -Stephen King";
lowerCaseString = secondQuote.toLowerCase();

alert("Lowercase string is " + lowerCaseString)
alert(`character at the 3rd position of secondQuote is` + secondQuote.charAt(3)))


findString = secondQuote.slice(34)


console.log(findString)
